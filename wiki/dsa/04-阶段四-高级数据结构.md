---
title: 阶段四 - 高级数据结构
tags: [dsa, phase-4, hash, trie, union-find, lru]
date: 2026-05-07
sources: [dsa/00-总览]
---

# 阶段四：高级数据结构（第 10-12 周）

> 目标：掌握面试高频的高级数据结构，能手写 LRU 缓存

---

## 1. 哈希表深入

### 原理

```
key → hash(key) → index → bucket[index] 存放 (key, value)
```

- 哈希冲突：两个不同的 key 映射到同一个 index
- 解决方案：链地址法（Java HashMap）/ 开放寻址法
- 负载因子：元素数 / 桶数，超过阈值（0.75）就扩容

### 手写简化版 HashMap

```java
class MyHashMap {
    private static class Node {
        int key, value;
        Node next;
        Node(int k, int v) { key = k; value = v; }
    }

    private final Node[] buckets;
    private static final int SIZE = 10007;  // 质数减少冲突

    public MyHashMap() {
        buckets = new Node[SIZE];
    }

    private int hash(int key) {
        return key % SIZE;
    }

    public void put(int key, int value) {
        int idx = hash(key);
        Node prev = findPrev(buckets[idx], key);
        if (prev.next != null) {
            prev.next.value = value;  // 更新
        } else {
            Node node = new Node(key, value);
            node.next = buckets[idx];
            buckets[idx] = node;  // 头插
        }
    }

    public int get(int key) {
        int idx = hash(key);
        Node prev = findPrev(buckets[idx], key);
        return prev.next != null ? prev.next.value : -1;
    }

    public void remove(int key) {
        int idx = hash(key);
        Node prev = findPrev(buckets[idx], key);
        if (prev.next != null) prev.next = prev.next.next;
    }

    private Node findPrev(Node head, int key) {
        if (head == null) return new Node(-1, -1);  // dummy
        Node prev = new Node(-1, -1);
        prev.next = head;
        Node curr = prev;
        while (curr.next != null) {
            if (curr.next.key == key) return curr;
            curr = curr.next;
        }
        return curr;
    }
}
```

---

## 2. 并查集（Union-Find）

**用途**：判断两个元素是否在同一个集合中（连通性问题）

```java
class UnionFind {
    int[] parent;
    int[] rank;

    UnionFind(int n) {
        parent = new int[n];
        rank = new int[n];
        for (int i = 0; i < n; i++) parent[i] = i;  // 初始时自己是自己的父
    }

    // 查找根节点（路径压缩）
    int find(int x) {
        if (parent[x] != x) parent[x] = find(parent[x]);
        return parent[x];
    }

    // 合并两个集合（按秩合并）
    boolean union(int x, int y) {
        int px = find(x), py = find(y);
        if (px == py) return false;  // 已在同一集合
        if (rank[px] < rank[py]) parent[px] = py;
        else if (rank[px] > rank[py]) parent[py] = px;
        else { parent[py] = px; rank[px]++; }
        return true;
    }

    boolean connected(int x, int y) {
        return find(x) == find(y);
    }
}
```

**应用**：
- 岛屿数量（另一种解法）
- 冗余连接
- 朋友圈/连通分量

---

## 3. Trie（前缀树）

**用途**：高效的字符串前缀查找

```java
class Trie {
    private Trie[] children = new Trie[26];
    private boolean isEnd;

    void insert(String word) {
        Trie node = this;
        for (char c : word.toCharArray()) {
            int idx = c - 'a';
            if (node.children[idx] == null) node.children[idx] = new Trie();
            node = node.children[idx];
        }
        node.isEnd = true;
    }

    boolean search(String word) {
        Trie node = searchPrefix(word);
        return node != null && node.isEnd;
    }

    boolean startsWith(String prefix) {
        return searchPrefix(prefix) != null;
    }

    private Trie searchPrefix(String prefix) {
        Trie node = this;
        for (char c : prefix.toCharArray()) {
            int idx = c - 'a';
            if (node.children[idx] == null) return null;
            node = node.children[idx];
        }
        return node;
    }
}
```

**时间复杂度**：插入/查找都是 O(L)，L 是字符串长度。比 HashMap 的优势：支持前缀搜索。

---

## 4. LRU 缓存（面试高频）

**要求**：O(1) 的 get 和 put

**思路**：HashMap + 双向链表

```java
class LRUCache {
    private final int capacity;
    private final Map<Integer, Node> map;
    private final Node head, tail;  // dummy 节点

    class Node {
        int key, value;
        Node prev, next;
        Node(int k, int v) { key = k; value = v; }
    }

    public LRUCache(int capacity) {
        this.capacity = capacity;
        this.map = new HashMap<>();
        head = new Node(0, 0);
        tail = new Node(0, 0);
        head.next = tail;
        tail.prev = head;
    }

    public int get(int key) {
        if (!map.containsKey(key)) return -1;
        Node node = map.get(key);
        moveToHead(node);  // 标记为最近使用
        return node.value;
    }

    public void put(int key, int value) {
        if (map.containsKey(key)) {
            Node node = map.get(key);
            node.value = value;
            moveToHead(node);
        } else {
            Node node = new Node(key, value);
            map.put(key, node);
            addToHead(node);
            if (map.size() > capacity) {
                Node removed = removeTail();
                map.remove(removed.key);
            }
        }
    }

    private void addToHead(Node node) {
        node.prev = head;
        node.next = head.next;
        head.next.prev = node;
        head.next = node;
    }

    private void removeNode(Node node) {
        node.prev.next = node.next;
        node.next.prev = node.prev;
    }

    private void moveToHead(Node node) {
        removeNode(node);
        addToHead(node);
    }

    private Node removeTail() {
        Node node = tail.prev;
        removeNode(node);
        return node;
    }
}
```

**Java 工程师提示**：Java 内置 `LinkedHashMap` 可以直接实现 LRU，但面试要求手写。

```java
// 用 LinkedHashMap 的简单实现
class LRUCacheSimple extends LinkedHashMap<Integer, Integer> {
    private final int capacity;
    public LRUCacheSimple(int capacity) {
        super(capacity, 0.75f, true);  // accessOrder=true
        this.capacity = capacity;
    }
    protected boolean removeEldestEntry(Map.Entry<Integer, Integer> eldest) {
        return size() > capacity;
    }
}
```

---

## 5. 线段树（了解即可）

**用途**：区间查询 + 区间修改，O(log n)

```java
// 区间求和
class SegmentTree {
    int[] tree;
    int n;

    SegmentTree(int[] arr) {
        n = arr.length;
        tree = new int[4 * n];
        build(arr, 0, 0, n - 1);
    }

    void build(int[] arr, int node, int start, int end) {
        if (start == end) { tree[node] = arr[start]; return; }
        int mid = (start + end) / 2;
        build(arr, 2 * node + 1, start, mid);
        build(arr, 2 * node + 2, mid + 1, end);
        tree[node] = tree[2 * node + 1] + tree[2 * node + 2];
    }

    int query(int l, int r) { return query(0, 0, n - 1, l, r); }

    int query(int node, int start, int end, int l, int r) {
        if (r < start || end < l) return 0;
        if (l <= start && end <= r) return tree[node];
        int mid = (start + end) / 2;
        return query(2 * node + 1, start, mid, l, r)
             + query(2 * node + 2, mid + 1, end, l, r);
    }
}
```

**面试中出现频率不高，但大厂偶尔考。了解原理即可。**

---

## 练习

1. 两数之和（LeetCode 1）— HashMap 应用
2. LRU 缓存（LeetCode 146）— 手写
3. 实现 Trie（LeetCode 208）
4. 冗余连接（LeetCode 684）— 并查集
5. 岛屿数量（LeetCode 200）— 并查集解法
6. 前缀树（LeetCode 211）— 支持正则匹配的 Trie
7. 最长连续序列（LeetCode 128）— HashSet

---

## 检验标准

- [ ] 能手写简化版 HashMap
- [ ] 掌握并查集的 find + union 操作
- [ ] 能实现 Trie 的插入/查找/前缀匹配
- [ ] 能手写 LRU 缓存（HashMap + 双向链表）
- [ ] 了解线段树的用途和基本结构
